#include <stdio.h>

int main(void) {
	int t;
  float x,y,a,b,i,j;
	scanf("%d",&t);
	while(t--)
	{
	  scanf("%f %f %f %f",&a,&b,&x,&y);
    i=x/a;
    j=y/b;
	  if(i<j)
      printf("-1\n");
    else if(i>j)
      printf("1\n");
    else
      printf("0\n");
	}
	return 0;
}
